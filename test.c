int main() {
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z; 
    return z + x + y;
}
int main() {
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z; 
    return z + x + y;
}
int main() {
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z; 
    return z + x + y;
}
int main() {
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
     int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int z = 2+3 + 4 * 5 / 6 * 2;
    int x = z * z + z * z / z;
    int y = x + x + z * z;
    int x = z * z + z * z / z; 
    return z + x + y;
}
